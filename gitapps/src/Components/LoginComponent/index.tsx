import { FormContainer, LoginContainer, LogoContainer } from "./styles";

function Login() {
  return (
    <LoginContainer>
      <LogoContainer>a</LogoContainer>
      <FormContainer>a</FormContainer>
    </LoginContainer>
  );
}

export default Login;
